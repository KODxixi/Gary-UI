# 完整 Demo 基线向全系统对齐 · 2026-09-08

本轮修复的是 HTML Portal、模板、组件与制图目录的视觉不一致。完整 Demo 作为基线；没有将本轮局部验收表述成整套业务功能无遗留通过。

## 变更

- Portal、应用模式、页面模板、起手模板和两套制图目录接入共享卡片材质与折射；30px 默认圆角，文字不参与滤镜，实底板禁用折射。样例外框使用普通分组，不再叠套玻璃。
- 复制、打开、重播、主题、菜单、关注等工具操作改为圆形本地 Lucide 图标；状态更新保留 SVG、可访问名称和悬停说明。导航、分段筛选及内容选择保留文字。
- 删除 dot-grid.js 的线网绘制、material.js 的装饰等高线及闲置线网参数。每个页面只有一套点阵；Canvas 成功绘制后 CSS 后备点阵关闭。嵌入页使用自身纯色底，不透出第二套页面点阵。
- 起手模板生成代码、制图目录生成器与母本构建产物同步。README、AGENTS、DESIGN、SKILL 和按钮契约同步。

## 已执行

- `node scripts/card_material_qa.mjs`：100/100；12 个 Portal 栏目、12 个独立入口，1440/390 CSS px，暗/亮主题，以及四组真实按钮状态交互。
- 浏览器 Canvas 绘制插桩：每个被测页面点阵 fill > 0、stroke = 0；可见 Scene = 1、Canvas = 1、CSS 背景图 = none；无装饰等高线 Canvas。
- 额外实测鼠标邻域点阵反馈：普通动态会变化，减弱动态保持静态，见 `dot-motion.json`。
- 检查说明卡、材质样片及普通卡片是否遗漏共享材质接入，并检查可见圆形动作 SVG、名称、44×44px、50% 圆角；普通卡片折射状态、文字 filter=none、无嵌套玻璃、无水平溢出、无页面脚本错误。
- `node scripts/demo_interaction_qa.mjs`：12/12，六个完整 Demo × 桌面/手机。该脚本不覆盖下述两项已知数据问题。
- Python 测试：174 passed，75 subtests passed；制图适配器：17 passed；`npm run build` 通过。
- 当前可见 IAB 中检查组件页圆形动作、简化控件与纯点阵，以及独立制图目录的卡片与导出入口；另检查手机浅色组件和手机暗色制图页截图。结构检测与目视检查分别记录，不以数量代替美学判断。
- 本次修改文件的普通 `git diff --check` 通过。全工作区仍有此前生成的第三方图表 HTML 尾空格，不在此次修改范围内。

完整数据见 [report.json](report.json)，截图命名为 `components/visuals-1440/390-dark/light.png`（实际文件采用连字符）。

## 边界

本轮未提交、推送或同步用户运行投影；仅更新 C:\AI\UI\gary-ui 母本及本地预览。未重新生成独立 SVG/PNG/PDF 图表文件，制图目录 index.html 已重建。

独立验收中的两项分析数据 P2（上海筛选后详情对象、当前筛选范围与静态导出不一致）仍待修复。Safari 和实体手机未验证。


## 入口纠正：Background 与深浅分离

此前把主题隐藏在“显示设置”中没有遵循完整 Demo。已改为顶栏纯文字 Background + 独立圆形深浅按钮；Background 直接复用 Demo 的共享功能卡。深色显示太阳、浅色显示月亮，图标表示切换目标。原视口、材质和背景动态覆盖设置移至材质页。

入口分离后 100/100 浏览器回归通过，覆盖 Background 开关、无主题控件嵌入、Escape 返回焦点、明暗主题和 390px。具体背景引擎与主题互不干扰的操作证据见 `background-separation.json`。
